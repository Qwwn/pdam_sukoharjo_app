package com.metromultindo.tirtamakmur.ui.contact

import androidx.lifecycle.ViewModel
import dagger.hilt.android.lifecycle.HiltViewModel
import javax.inject.Inject

@HiltViewModel
class ContactViewModel @Inject constructor() : ViewModel() {
    // This is a simple ViewModel for the Contact screen
    // In a real app, it might contain methods to fetch contact information
    // from an API or to send feedback/messages to the company

    // For now, it's empty as we're using static contact information
}